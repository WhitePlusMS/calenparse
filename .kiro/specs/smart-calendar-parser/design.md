# 设计文档

## 概述

智能日历解析器是一个纯前端单页应用，通过 LLM 解析通告文本并创建日程，提供完整的日程管理功能。

### 已实现的 MVP 核心功能 ✅

1. **输入通告文本** - 用户输入或粘贴通告（需求 1）
2. **LLM 解析** - 自动提取日程信息（需求 2）
3. **预览编辑** - 确认和修改解析结果（需求 4）
4. **日历展示** - 月视图显示日程（需求 3 部分）
5. **基本管理** - 查看、编辑、删除日程（需求 6）
6. **数据持久化** - Supabase 存储（需求 7）
7. **日期处理** - 支持多种日期格式（需求 5）
8. **错误处理** - 完善的错误提示和重试机制（需求 10 部分）

### 待实现功能 🚧

#### 高优先级功能
1. **多视图支持** - 日视图、周视图、年视图（需求 3）
2. **列表视图和时间轴** - 扁平化列表、时间轴展示（需求 8）
3. **统计分析** - 日程统计和可视化（需求 9）
4. **导入导出** - iCal/JSON 格式支持（需求 11）
5. **批量操作** - 多选、批量删除/编辑（需求 12）
6. **智能更新** - 通过新文本更新已有日程（需求 13）
7. **主题定制** - 深色/浅色模式、自定义颜色（需求 14）
8. **标签系统** - 为日程添加标签分类（需求 18）
9. **分享功能** - 生成分享文本/图片（需求 19）

#### 低优先级功能
15. **语音输入** - 语音转文字输入（需求 22）

### 技术栈

- **前端**：Vue 3 + TypeScript + Vite
- **UI**：Element Plus + Scoped CSS
- **数据库**：Supabase (PostgreSQL)
- **日历组件**：FullCalendar
- **日期处理**：Day.js
- **状态管理**：Composables (单例模式)

## 架构

### 简化架构图

```
┌──────────────────────────────────────┐
│         用户界面 (Vue 3)              │
│  输入面板 │ 日历视图 │ 事件对话框    │
└──────────────────────────────────────┘
                 │
┌──────────────────────────────────────┐
│         业务逻辑 (Composables)        │
│   useEvents │ useLLM │ useSupabase   │
└──────────────────────────────────────┘
                 │
┌──────────────────────────────────────┐
│            数据层                     │
│      Supabase │ LLM API              │
└──────────────────────────────────────┘
```

## 组件结构 (MVP)

```
src/
├── components/
│   ├── InputPanel.vue      # 输入和解析
│   ├── CalendarView.vue    # 月视图日历
│   ├── EventDialog.vue     # 事件详情/编辑
│   └── PreviewDialog.vue   # 解析结果预览
├── composables/
│   ├── useEvents.ts        # 事件CRUD
│   ├── useLLM.ts           # LLM解析
│   └── useSupabase.ts      # 数据库操作
├── stores/
│   └── eventStore.ts       # 事件状态
├── types/
│   └── index.ts            # 类型定义
├── utils/
│   └── date.ts             # 日期工具
├── App.vue
└── main.ts
```

## 数据模型 (MVP)

### Supabase 表结构

```sql
-- 只需要一个表
CREATE TABLE events (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  title TEXT NOT NULL,
  start_time TIMESTAMPTZ NOT NULL,
  end_time TIMESTAMPTZ NOT NULL,
  is_all_day BOOLEAN DEFAULT false,
  location TEXT,
  description TEXT,
  original_text TEXT,  -- 保存原始通告文本
  is_completed BOOLEAN DEFAULT false,  -- 新增：完成状态
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_events_start_time ON events(start_time);
CREATE INDEX idx_events_is_completed ON events(is_completed);  -- 新增：完成状态索引
```

### TypeScript 类型

```typescript
interface CalendarEvent {
  id: string;
  title: string;
  startTime: Date;
  endTime: Date;
  isAllDay: boolean;
  location?: string;
  description?: string;
  originalText?: string;
  isCompleted?: boolean;  // 新增：完成状态
  createdAt: Date;
  updatedAt: Date;
}

interface ParsedEvent {
  title?: string;
  startTime?: Date;
  endTime?: Date;
  isAllDay?: boolean;
  location?: string;
  description?: string;
}

interface LLMRequest {
  text: string;
}

interface LLMResponse {
  events: ParsedEvent[];
  error?: string;
}
```

## 核心接口

### 1. LLM 解析服务

```typescript
// composables/useLLM.ts
export function useLLM() {
  const parseText = async (text: string): Promise<ParsedEvent[]> => {
    // 调用 LLM API 解析文本
    // 返回解析结果
  };
  
  return { parseText };
}
```

### 2. 事件管理

```typescript
// composables/useEvents.ts
export function useEvents() {
  const events = ref<CalendarEvent[]>([]);
  
  const fetchEvents = async () => {
    // 从 Supabase 获取事件
  };
  
  const createEvent = async (event: Partial<CalendarEvent>) => {
    // 创建新事件
  };
  
  const updateEvent = async (id: string, updates: Partial<CalendarEvent>) => {
    // 更新事件
  };
  
  const deleteEvent = async (id: string) => {
    // 删除事件
  };
  
  return { events, fetchEvents, createEvent, updateEvent, deleteEvent };
}
```

### 3. Supabase 操作

```typescript
// composables/useSupabase.ts
import { createClient } from '@supabase/supabase-js';

const supabase = createClient(
  import.meta.env.VITE_SUPABASE_URL,
  import.meta.env.VITE_SUPABASE_ANON_KEY
);

export function useSupabase() {
  return { supabase };
}
```

## 用户流程 (MVP)

### 主流程

1. **输入** → 用户在输入框粘贴通告文本
2. **解析** → 点击"解析"按钮，调用 LLM API
3. **预览** → 显示解析结果，用户可以编辑
4. **确认** → 点击"创建"，保存到 Supabase
5. **展示** → 日历中显示新创建的日程

### 查看和编辑

1. **查看** → 点击日历中的事件，显示详情
2. **编辑** → 在详情对话框中修改，保存更新
3. **删除** → 点击删除按钮，确认后删除

## 正确性属性 (MVP 核心)

*一个属性是一个特征或行为，应该在系统的所有有效执行中保持为真——本质上是关于系统应该做什么的正式陈述。属性作为人类可读规范和机器可验证正确性保证之间的桥梁。*

### 属性 1：输入文本存储
*对于任何*有效的输入文本，系统存储的文本应该与用户输入完全一致
**验证需求：1.1**

### 属性 2：空输入拒绝
*对于任何*空字符串，系统应该拒绝提交并显示错误提示
**验证需求：1.3**

### 属性 3：解析结果不生成虚假数据
*对于任何*LLM 解析结果，如果某个字段无法识别，该字段应该为空
**验证需求：2.10**

### 属性 4：日程创建后可见
*对于任何*成功创建的日程，该事件应该在日历视图中可见
**验证需求：3.1**

### 属性 5：日程编辑反映
*对于任何*日程的编辑操作，保存后的修改应该立即在日历中显示
**验证需求：6.2**

### 属性 6：日程删除完整性
*对于任何*被删除的日程，该事件应该从日历和数据库中完全移除
**验证需求：6.3**

### 属性 7：数据持久化
*对于任何*日程数据，保存到 Supabase 后刷新页面，应该能重新加载
**验证需求：7.1, 7.2**

### 属性 39：完成状态切换
*对于任何*事件，切换完成状态后，该状态应该立即在所有视图中反映
**验证需求：23.2, 23.6**

### 属性 40：完成状态视觉区分
*对于任何*已完成的事件，在所有视图中应该有明显的视觉区分
**验证需求：23.3, 23.4, 23.7**

## 错误处理

### 简单策略

1. **LLM API 错误** - 显示错误消息，允许重试
2. **网络错误** - 提示用户检查网络
3. **数据验证错误** - 高亮错误字段，显示提示
4. **Supabase 错误** - 显示友好的错误消息

## 测试策略 (MVP)

### 手动测试

- 输入各种格式的通告文本
- 测试解析结果的准确性
- 测试创建、编辑、删除功能
- 测试页面刷新后数据保留

### 基本单元测试

- 日期格式化函数
- 数据验证函数

## 部署

### 环境变量

```
VITE_SUPABASE_URL=your-supabase-url
VITE_SUPABASE_ANON_KEY=your-anon-key
VITE_LLM_API_KEY=your-llm-api-key
VITE_LLM_API_ENDPOINT=your-llm-endpoint
```

### 构建和部署

```bash
# 安装依赖
npm install

# 开发
npm run dev

# 构建
npm run build

# 部署到 Vercel/Netlify
# 上传 dist 目录
```

## 待实现功能详细设计

### 1. 多视图支持（需求 3）

#### 属性 8：视图切换保持日期
*对于任何*视图切换操作，当前选中的日期应该在新视图中保持定位
**验证需求：3.9**

#### 属性 9：日视图时间段显示
*对于任何*日视图，应该按小时划分时间段显示当日日程
**验证需求：3.5**

#### 属性 10：周视图七天显示
*对于任何*周视图，应该显示完整的七天日程概览
**验证需求：3.6**

#### 属性 11：年视图密度分布
*对于任何*年视图，应该显示全年日程密度分布
**验证需求：3.8**

**实现方案：**
- 扩展 CalendarView 组件，添加视图切换按钮
- 使用 FullCalendar 的 timeGridDay、timeGridWeek、dayGridYear 视图
- 添加视图状态管理，保存当前视图和选中日期
- 实现视图间的平滑切换动画

**数据模型变化：** 无需修改

**新增组件：**
```typescript
// ViewSwitcher.vue - 视图切换器
interface ViewType {
  type: 'day' | 'week' | 'month' | 'year';
  label: string;
  icon: string;
}
```

---

### 2. 列表视图和时间轴（需求 8）

#### 属性 12：列表视图时间排序
*对于任何*列表视图，事件应该按时间顺序从最近到最远排列
**验证需求：8.2**

#### 属性 13：列表视图同步更新
*对于任何*在列表视图中的操作，应该同步更新到日历视图
**验证需求：8.5**

#### 属性 14：时间轴时间间隔显示
*对于任何*时间轴视图，应该显示事件之间的时间间隔
**验证需求：8.8**

#### 属性 15：热力图密度表示
*对于任何*热力图，颜色深浅应该准确表示日程密度
**验证需求：8.12**

**实现方案：**
- 创建 ListView.vue 组件，显示扁平化事件列表
- 创建 TimelineView.vue 组件，使用垂直时间轴布局
- 创建 HeatmapView.vue 组件，使用 Canvas 或 SVG 绘制热力图
- 添加视图切换逻辑，支持在不同视图间切换

**数据模型变化：** 无需修改

**新增组件：**
```typescript
// ListView.vue
interface ListViewProps {
  events: CalendarEvent[];
  sortOrder: 'asc' | 'desc';
}

// TimelineView.vue
interface TimelineViewProps {
  events: CalendarEvent[];
  orientation: 'vertical' | 'horizontal';
}

// HeatmapView.vue
interface HeatmapViewProps {
  events: CalendarEvent[];
  granularity: 'hour' | 'day' | 'week';
  dateRange: [Date, Date];
}
```

---

### 3. 统计分析（需求 9）

#### 属性 16：统计数据准确性
*对于任何*统计计算，结果应该与实际事件数据完全一致
**验证需求：9.2, 9.3**

#### 属性 17：时间范围过滤
*对于任何*自定义时间范围，统计应该只包含该范围内的事件
**验证需求：9.4**

**实现方案：**
- 创建 StatisticsView.vue 组件
- 使用 Chart.js 或 ECharts 进行数据可视化
- 实现时间段分布统计（按日/周/月）
- 实现地点分布统计（饼图或柱状图）
- 添加自定义时间范围选择器

**数据模型变化：** 无需修改

**新增组件：**
```typescript
// StatisticsView.vue
interface Statistics {
  totalEvents: number;
  timeDistribution: { period: string; count: number }[];
  locationDistribution: { location: string; count: number }[];
  eventTypeDistribution: { type: string; count: number }[];
}

// composables/useStatistics.ts
export function useStatistics() {
  const calculateStatistics = (
    events: CalendarEvent[],
    startDate?: Date,
    endDate?: Date
  ): Statistics => { /* ... */ };
}
```

---

### 4. 导入导出（需求 11）

#### 属性 18：导出数据完整性
*对于任何*导出操作，导出的数据应该包含所有事件的完整信息
**验证需求：11.2, 11.3**

#### 属性 19：导入数据验证
*对于任何*导入操作，应该验证数据格式并拒绝无效数据
**验证需求：11.7**

#### 属性 20：备份恢复一致性
*对于任何*备份恢复操作，恢复后的数据应该与备份时完全一致
**验证需求：11.10**

**实现方案：**
- 创建 ImportExport.vue 组件或集成到设置中
- 实现 iCal (.ics) 格式导出（使用 ics.js 库）
- 实现 JSON 格式导出
- 实现文件导入解析和验证
- 添加冲突处理逻辑（保留/覆盖/合并）
- 实现备份下载和恢复上传

**数据模型变化：** 无需修改

**新增工具函数：**
```typescript
// utils/import-export.ts
export function exportToICalendar(events: CalendarEvent[]): string;
export function exportToJSON(events: CalendarEvent[]): string;
export function importFromICalendar(icsContent: string): ParsedEvent[];
export function importFromJSON(jsonContent: string): CalendarEvent[];
export function createBackup(events: CalendarEvent[]): Blob;
export function restoreBackup(file: File): Promise<CalendarEvent[]>;
```

---

### 5. 批量操作（需求 12）

#### 属性 21：批量选择状态
*对于任何*批量选择操作，选中状态应该在所有视图中保持一致
**验证需求：12.2**

#### 属性 22：批量修改原子性
*对于任何*批量修改操作，要么全部成功，要么全部失败
**验证需求：12.5**

**实现方案：**
- 添加批量操作模式切换
- 在日历和列表视图中添加复选框
- 创建批量操作工具栏
- 实现批量删除（带确认）
- 实现批量编辑对话框（修改共同字段）
- 添加全选/取消全选功能

**数据模型变化：**
```typescript
// 添加选择状态管理
interface SelectionState {
  isSelectionMode: boolean;
  selectedIds: Set<string>;
}
```

**新增组件：**
```typescript
// BatchOperationBar.vue
interface BatchOperationBarProps {
  selectedCount: number;
  onDelete: () => void;
  onEdit: () => void;
  onCancel: () => void;
}

// BatchEditDialog.vue
interface BatchEditDialogProps {
  events: CalendarEvent[];
  onSave: (updates: Partial<CalendarEvent>) => void;
}
```

---

### 6. 智能更新（需求 13）

#### 属性 23：事件匹配准确性
*对于任何*智能更新操作，应该准确匹配到正确的已有事件
**验证需求：13.2, 13.3**

#### 属性 24：冲突信息标注
*对于任何*信息冲突，应该明确标注冲突字段并让用户选择
**验证需求：13.8**

**实现方案：**
- 扩展 LLM 解析，添加"更新模式"
- 实现事件匹配算法（基于标题、时间、关键词）
- 创建更新预览对话框，显示原有信息和新信息对比
- 高亮显示将要更新的字段
- 处理多个匹配候选的情况
- 处理无匹配时的新建提示

**数据模型变化：**
```typescript
interface UpdateMatch {
  existingEvent: CalendarEvent;
  newInfo: ParsedEvent;
  matchScore: number;
  conflicts: { field: string; oldValue: any; newValue: any }[];
}
```

**新增组件：**
```typescript
// UpdatePreviewDialog.vue
interface UpdatePreviewDialogProps {
  matches: UpdateMatch[];
  onConfirm: (selectedMatch: UpdateMatch, updates: Partial<CalendarEvent>) => void;
  onCreateNew: () => void;
}
```

---

### 7. 主题定制（需求 14）

#### 属性 25：主题持久化
*对于任何*主题设置，应该保存到本地存储并在重新打开时恢复
**验证需求：14.5, 14.6**

**实现方案：**
- 创建 ThemeSettings.vue 组件
- 实现深色/浅色模式切换
- 使用 CSS 变量定义主题颜色
- 为不同事件类型提供颜色选择器
- 将主题设置保存到 localStorage
- 在应用启动时加载主题设置

**数据模型变化：**
```typescript
interface ThemeSettings {
  mode: 'light' | 'dark';
  eventColors: {
    [eventType: string]: string;
  };
  primaryColor?: string;
  accentColor?: string;
}
```

**新增工具函数：**
```typescript
// composables/useTheme.ts
export function useTheme() {
  const theme = ref<ThemeSettings>(loadTheme());
  const applyTheme = (settings: ThemeSettings) => { /* ... */ };
  const saveTheme = (settings: ThemeSettings) => { /* ... */ };
}
```

---

### 8. 标签系统（需求 18）

#### 属性 31：标签过滤准确性
*对于任何*标签过滤操作，应该只显示包含该标签的事件
**验证需求：18.5**

#### 属性 32：标签自动补全
*对于任何*标签输入，应该提供已有标签的自动补全建议
**验证需求：18.2**

**实现方案：**
- 创建 TagManager.vue 组件
- 在事件编辑对话框中添加标签输入
- 实现标签自动补全
- 为标签添加颜色选择
- 在日历视图中显示标签
- 实现标签过滤功能
- 扩展 LLM 提示，尝试识别和建议标签

**数据模型变化：**
```typescript
interface Tag {
  id: string;
  name: string;
  color: string;
}

interface CalendarEvent {
  // ... existing fields
  tags?: string[]; // Array of tag IDs
}

// Supabase schema update
CREATE TABLE tags (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  name TEXT NOT NULL UNIQUE,
  color TEXT NOT NULL,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

ALTER TABLE events ADD COLUMN tag_ids TEXT[];
```

---

### 9. 分享功能（需求 19）

#### 属性 33：分享内容完整性
*对于任何*分享操作，生成的内容应该包含所有关键信息
**验证需求：19.2**

**实现方案：**
- 创建 ShareDialog.vue 组件
- 实现文本格式分享（复制到剪贴板）
- 实现图片格式分享（使用 html2canvas）
- 支持单个事件和多个事件分享
- 格式化分享内容（包含标题、时间、地点、描述）
- 处理重复日程的分享说明

**新增组件：**
```typescript
// ShareDialog.vue
interface ShareDialogProps {
  events: CalendarEvent[];
  format: 'text' | 'image';
}

// utils/share.ts
export function generateShareText(events: CalendarEvent[]): string;
export function generateShareImage(events: CalendarEvent[]): Promise<Blob>;
```

---

### 10. 语音输入（需求 22 - 低优先级）

#### 属性 38：语音识别准确性
*对于任何*语音输入，转换后的文字应该填充到输入框
**验证需求：22.4**

**实现方案：**
- 使用 Web Speech API (SpeechRecognition)
- 在 InputPanel 中添加语音输入按钮
- 请求麦克风权限
- 显示录音状态指示器
- 实时或录音结束后转换为文字
- 处理浏览器兼容性（检测 API 支持）
- 处理识别错误和重试

**新增组件：**
```typescript
// VoiceInput.vue
interface VoiceInputProps {
  onTranscript: (text: string) => void;
  onError: (error: Error) => void;
}

// composables/useVoiceInput.ts
export function useVoiceInput() {
  const isSupported = ref(false);
  const isRecording = ref(false);
  const startRecording = () => { /* ... */ };
  const stopRecording = () => { /* ... */ };
}
```

---

### 11. 任务完成状态（需求 23）

#### 属性 39：完成状态切换
*对于任何*事件，切换完成状态后，该状态应该立即在所有视图中反映
**验证需求：23.2, 23.6**

#### 属性 40：完成状态视觉区分
*对于任何*已完成的事件，在所有视图中应该有明显的视觉区分
**验证需求：23.3, 23.4, 23.7**

**实现方案：**
- 在数据库中添加 `is_completed` 字段
- 在 TypeScript 类型中添加 `isCompleted` 属性
- 在事件编辑对话框中添加完成状态复选框
- 在日历视图中为已完成事件添加特殊样式（半透明、删除线）
- 在列表视图中添加可点击的完成状态复选框
- 实现快速切换完成状态的功能
- 添加按完成状态筛选的选项

**数据模型变化：**
```typescript
interface CalendarEvent {
  // ... existing fields
  isCompleted?: boolean; // 新增：完成状态，默认 false
}

// Supabase schema update
ALTER TABLE events ADD COLUMN is_completed BOOLEAN DEFAULT false;
CREATE INDEX idx_events_is_completed ON events(is_completed);
```

**视觉样式设计：**
```css
/* 已完成事件样式 */
.event--completed {
  opacity: 0.6;
  text-decoration: line-through;
  background: var(--completed-bg);
  border-left: 4px solid var(--success-color);
}

/* 完成状态复选框 */
.event-checkbox {
  cursor: pointer;
  transition: all 0.3s ease;
}

.event-checkbox--completed {
  color: var(--success-color);
}
```

**新增功能：**
```typescript
// composables/useEvents.ts
export function useEvents() {
  // ... existing methods
  
  const toggleEventCompletion = async (id: string, isCompleted: boolean) => {
    await updateEvent(id, { isCompleted });
  };
  
  return {
    // ... existing returns
    toggleEventCompletion,
  };
}
```

---

## 开发优先级和路线图

### 第一阶段：MVP（已完成 ✅）
1. ✅ 项目框架和基础配置
2. ✅ 输入面板和 LLM 解析
3. ✅ 预览对话框
4. ✅ 日历月视图
5. ✅ 事件 CRUD 操作
6. ✅ Supabase 数据持久化
7. ✅ 错误处理和通知

### 第二阶段：核心扩展功能
1. 多视图支持（日/周/年视图）
2. 列表视图和时间轴
3. 标签系统
4. 导入导出（iCal/JSON）

### 第三阶段：高级功能
5. 统计分析和可视化
6. 批量操作
7. 智能更新已有日程

### 第四阶段：用户体验优化
8. 主题定制
9. 分享功能

### 第五阶段：可选功能
10. 语音输入（低优先级）
